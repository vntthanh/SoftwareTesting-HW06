# Pool C Postman Conversion Report

## Conversion summary

- Source: `test-cases/c-order-management.csv` (85 final reviewed rows).
- Review context: `review/pool-c/`.
- Collection: `postman/pool-c-order-management.postman_collection.json`.
- Mapping: 79 single-request cases plus six ordered flows, totaling 85 cases and 93 requests.
- Ordered flows: API-080 (2), API-081 (2), API-082 (2), API-083 (2), API-084 (4), API-085 (2).
- Categories: CONTRACT 24; DOMAIN 28; STATE 22; SECURITY 11.
- Reviewed expected results are copied unchanged into each request description and this report.
- No Newman execution was performed.

## Runtime and compatibility

- `postman/pool_c_fixtures.py` owns an isolated target and unrelated order per case, supplies deterministic Admin/non-Admin and invalid JWT variants, and supports post-request state inspection.
- `postman/run_pool_c_with_fixtures.py` snapshots the complete orders table, resets once per reviewed case (not between flow steps), and restores in `finally`.
- Collection-level pre-request logic upserts `X-Student-Id: {{studentId}}`, requires an authenticated loopback fixture controller, and skips unsafe unwrapped execution.
- Scripts use Newman-supported Collection v2.1 features: `pm.sendRequest`, `pm.test`, variables, request headers, and `pm.execution.skipRequest`.
- Full official Postman v2.1 schema status is recorded in `postman/pool-c-validation-results.json`.

## Assertion policy

Semantic database-state assertions are generated only where the reviewed result specifies a transition or no-mutation outcome. Exact `4xx` assertions occur only in human cases API-080–API-085. Exploratory/characterization cases retain response logging without invented pass/fail behavior. The contextual relevance of an ‘appropriate message’ remains a manual oracle; only presence of a non-empty error/message is automated.

## Row-to-request traceability

| Test ID | Category | Requests | Preconditions (preserved) | Reviewed expected result (unchanged) | Generated assertions / unresolved notes |
|---|---|---:|---|---|---|
| API-001 | CONTRACT | 1 | An existing order is in pending state; its concrete fixture ID is known; a valid JWT carrying role = 'admin' is available. | The order status is updated from pending to confirmed. Record the response, but do not assert an HTTP status, response media type, header, body schema, field, or message. | X-Student-Id header presence; fixture-backed target state |
| API-002 | CONTRACT | 1 | Use the otherwise valid pending-to-confirmed baseline fixture and a valid Admin JWT. | Record whether and how the request is handled. The selected status-update operation is documented only for PUT; no rejection status, Allow header, body, or message is asserted. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-003 | CONTRACT | 1 | A valid Admin JWT is available. | Record the observed routing and response behavior. The documented operation requires a concrete :id segment, but no HTTP status, error schema, or message is asserted for the altered path. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-004 | CONTRACT | 1 | A fixture value is confirmed not to identify any existing order; a valid Admin JWT is available. | Record the observed behavior only. Do not assert acceptance, rejection, status, body, message, or state effect because nonexistent-order behavior is unspecified. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-005 | CONTRACT | 1 | An existing pending order is available. | The request is not authorized to update the order. Record the response without asserting an HTTP status, header, body, schema, or message. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-006 | CONTRACT | 1 | An existing pending order and a valid Admin JWT value are available. | The request does not meet the documented Bearer authorization prerequisite and must not update the order. Record the response without asserting wire-level error details. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-007 | CONTRACT | 1 | An existing pending order and a controlled token known to be invalid are available. | The request is not authorized and the order status is not updated. Record the response without asserting an HTTP status, body, error field, or message. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-008 | CONTRACT | 1 | An existing pending order and a valid JWT for a non-admin account are available. | Token presence alone does not authorize the request, and the order status is not updated. Record the response without asserting its wire-level form. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-009 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | Record acceptance or rejection and the observed response. Do not assert status, parsing behavior, state effect, error schema, or message because malformed-JSON handling is unspecified. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-010 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | Record acceptance or rejection, any state effect, and the observed response without asserting a status, schema, or message. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-011 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | Record acceptance or rejection, state effect, and response. Do not assert a status, default destination, schema, or message. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-012 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | Record acceptance or rejection, state effect, and response. Do not assert a status, default, schema, or message. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-013 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | Record acceptance or rejection, state effect, and response without asserting null rejection, coercion, status, schema, or message. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-014 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | Record acceptance or rejection, state effect, and response without asserting type rejection, coercion, status, schema, or message. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-015 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | The unsupported value does not produce a documented lifecycle update. Record the observed response without asserting an HTTP status, error body, exact message, normalization behavior, or rejection precedence. | X-Student-Id header presence; fixture-backed target state |
| API-016 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | Record whether the extra member is accepted, ignored, or rejected and capture any state effect and response. No additional-properties policy or wire-level outcome is asserted. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-017 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available. | Record parser interpretation, acceptance or rejection, any state effect, and the response. Do not assert first-key wins, last-key wins, rejection, status, schema, or message. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-018 | CONTRACT | 1 | An existing order is in pending state; a valid Admin JWT is available. | The order status is updated from pending to canceled. Record the response without asserting any HTTP status, response schema, field, media type, header, or message. | X-Student-Id header presence; fixture-backed target state |
| API-019 | CONTRACT | 1 | An existing order is in shipping state; a valid Admin JWT is available. | The order is not moved to canceled. The request returns a semantic error with an appropriate message. Do not assert an HTTP status, media type, error schema, field name, exact wording, or objective message predicate beyond contextual relevance to the invalid transition. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-020 | CONTRACT | 1 | An existing order is in delivered state; a valid Admin JWT is available. | The delivered order remains delivered. The attempted transition returns a semantic error with an appropriate message, without asserting any HTTP status, exact error structure, field, or wording. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-021 | CONTRACT | 1 | An existing order is in canceled state; a valid Admin JWT is available. | The canceled order remains canceled. The attempted transition returns a semantic error with an appropriate message, without asserting any HTTP status, exact error structure, field, or wording. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-022 | CONTRACT | 1 | An existing order is in confirmed state; a valid Admin JWT is available. | Record whether the request succeeds, errors, or has another observable effect. Produce no conformance pass/fail verdict and assert no status, body, message, idempotency, or mutation outcome. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-023 | CONTRACT | 1 | An existing pending order and a valid Admin JWT are available; the client can send raw JSON bytes without a Content-Type header. | Record parsing, acceptance or rejection, any state effect, and the response. Produce no conformance pass/fail verdict for media-type handling and assert no status, schema, or message. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-024 | DOMAIN | 1 | A fixture order exists in pending state and its identifier is used as <existing-pending-order-id>. A valid JWT with role = 'admin' is available. | Authentication and authorization permit the order status update from pending to confirmed to succeed. No HTTP status code, response schema, headers, media type, or message is asserted. | X-Student-Id header presence; fixture-backed target state |
| API-025 | DOMAIN | 1 | A fixture order exists in delivered state and its identifier is used as <existing-delivered-order-id>. A valid Admin JWT is available. | The delivered-to-confirmed transition is rejected with an error carrying an appropriate message, and the order remains delivered. The exact HTTP status, response schema, and wording are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-026 | DOMAIN | 1 | <nonexistent-routable-id> is guaranteed not to identify an order. A valid Admin JWT is available. | No pass/fail conformance oracle exists. Record the actual response and any observed state effect; do not prescribe an HTTP status, schema, message, or nonexistent-order outcome. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-027 | DOMAIN | 1 | A valid Admin JWT is available. No order fixture is required because no id value can be delivered to the documented operation. | No operation-level conformance oracle exists. Record routing behavior only; do not classify it as id validation or prescribe any HTTP response. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-028 | DOMAIN | 1 | A valid Admin JWT is available. The fixture environment records whether an order can be associated with id lexical form abc. | No pass/fail conformance oracle exists because id syntax and type are unspecified. Record the actual response and state effect without declaring the lexical form valid or invalid. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-029 | DOMAIN | 1 | A valid Admin JWT is available. No fixture order is assigned the literal identifier null unless the implementation's identifier domain permits it. | No pass/fail conformance oracle exists. Record actual behavior; do not treat the string null as JSON null or prescribe identifier semantics. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-030 | DOMAIN | 1 | A fixture order exists in pending state. Capture its state before the request. | Access is not granted and the order status is not changed. Exact HTTP status, response schema, and message are unspecified. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-031 | DOMAIN | 1 | A fixture order exists in pending state. Capture its state before the request. | Access is not granted and the order status is not changed. Exact response details are unspecified. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-032 | DOMAIN | 1 | A fixture order exists in pending state. Capture its state before the request. | Access is not granted and the order status is not changed. Exact parser behavior and response details are unspecified. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-033 | DOMAIN | 1 | A fixture order exists in pending state. <invalid-signature-admin-JWT> is well-formed enough for the fixture but has an invalid signature. Capture order state before the request. | Access is not granted and the order status is not changed. Exact validation taxonomy, precedence, HTTP status, and body are unspecified. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-034 | DOMAIN | 1 | A fixture order exists in pending state. <valid-non-admin-JWT> is valid but does not contain role = 'admin'. Capture order state before the request. | Access is not granted and the order status is not changed. Exact HTTP status, response schema, and message are unspecified. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-035 | DOMAIN | 1 | A fixture order exists in confirmed state. A valid Admin JWT is available. | The confirmed-to-shipping update succeeds. No exact response status or schema is asserted. | X-Student-Id header presence; fixture-backed target state |
| API-036 | DOMAIN | 1 | A fixture order exists in shipping state. A valid Admin JWT is available. | The shipping-to-delivered update succeeds. No exact response status or schema is asserted. | X-Student-Id header presence; fixture-backed target state |
| API-037 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. | The pending-to-canceled update succeeds. No exact response details are asserted. | X-Student-Id header presence; fixture-backed target state |
| API-038 | DOMAIN | 1 | A fixture order exists in confirmed state. A valid Admin JWT is available. | The confirmed-to-pending transition is rejected with an error carrying an appropriate message, and the order remains confirmed. Exact response details are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-039 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | The value is not accepted as a valid destination and the order does not transition to unknown. Exact error status, schema, and message are unspecified. | X-Student-Id header presence; fixture-backed target state |
| API-040 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | The empty string is not accepted as a valid destination and the order state is unchanged. Exact response details are unspecified. | X-Student-Id header presence; fixture-backed target state |
| API-041 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No pass/fail conformance oracle exists. Record whether normalization, rejection, or other behavior occurs; do not require any HTTP status, schema, or message. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-042 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No pass/fail conformance oracle exists because nullability and formal schema behavior are unspecified. Record actual response and state effect. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-043 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No pass/fail conformance oracle exists because formal type and coercion rules are unspecified. Record actual response and state effect. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-044 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No pass/fail conformance oracle exists because status requiredness and default behavior are unspecified. Record actual response and state effect. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-045 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No pass/fail conformance oracle exists because body requiredness and empty-body behavior are unspecified. Record actual response and state effect. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-046 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No exact conformance response oracle exists. Record parser behavior and state effect; malformed JSON cannot represent the documented request example. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-047 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No pass/fail conformance oracle exists because a top-level object is implied only by example, not formally required. Record actual response and state effect. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-048 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No pass/fail conformance oracle exists. Record whether the additional property is ignored, rejected, or otherwise handled, plus the state effect. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-049 | DOMAIN | 1 | A fixture order exists in pending state. A valid Admin JWT is available. Capture order state before the request. | No pass/fail conformance oracle exists. Record whether the request is rejected or which duplicate value is effective, together with any state effect; prescribe no response details. | X-Student-Id header presence; reviewed response remains observational; no behavior oracle invented |
| API-050 | STATE | 1 | A fixture order exists in pending state with a known ID; a valid Bearer JWT whose role is admin is available. | The status change is accepted and the order reaches confirmed. The exact HTTP status, response headers, and response body are unspecified. | X-Student-Id header presence; fixture-backed target state |
| API-051 | STATE | 1 | A fixture order exists in confirmed state with a known ID; a valid Bearer JWT whose role is admin is available. | The status change is accepted and the order reaches shipping. The exact HTTP status, response headers, and response body are unspecified. | X-Student-Id header presence; fixture-backed target state |
| API-052 | STATE | 1 | A fixture order exists in shipping state with a known ID; a valid Bearer JWT whose role is admin is available. | The status change is accepted and the order reaches delivered. The exact HTTP status, response headers, and response body are unspecified. | X-Student-Id header presence; fixture-backed target state |
| API-053 | STATE | 1 | A fixture order exists in pending state with a known ID; a valid Bearer JWT whose role is admin is available. | The status change is accepted and the order reaches canceled. The exact HTTP status, response headers, and response body are unspecified. | X-Student-Id header presence; fixture-backed target state |
| API-054 | STATE | 1 | A fixture order exists in confirmed state with a known ID; a valid Bearer JWT whose role is admin is available. | The status change is accepted and the order reaches canceled. The exact HTTP status, response headers, and response body are unspecified. | X-Student-Id header presence; fixture-backed target state |
| API-055 | STATE | 1 | A fixture order exists in final state delivered with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-056 | STATE | 1 | A fixture order exists in final state delivered with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-057 | STATE | 1 | A fixture order exists in final state delivered with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-058 | STATE | 1 | A fixture order exists in final state delivered with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-059 | STATE | 1 | A fixture order exists in final state canceled with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-060 | STATE | 1 | A fixture order exists in final state canceled with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-061 | STATE | 1 | A fixture order exists in final state canceled with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-062 | STATE | 1 | A fixture order exists in final state canceled with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-063 | STATE | 1 | A fixture order exists in pending state with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-064 | STATE | 1 | A fixture order exists in pending state with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-065 | STATE | 1 | A fixture order exists in confirmed state with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-066 | STATE | 1 | A fixture order exists in confirmed state with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-067 | STATE | 1 | A fixture order exists in shipping state with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-068 | STATE | 1 | A fixture order exists in shipping state with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-069 | STATE | 1 | A fixture order exists in shipping state with a known ID; a valid Bearer JWT whose role is admin is available. | The transition is rejected with an error and an appropriate message. The exact HTTP status, response schema, wording, and post-error persistence behavior are unspecified. | X-Student-Id header presence; denial/error signal; non-empty error/message; contextual appropriateness is manual; fixture-backed target state |
| API-070 | SECURITY | 1 | A controlled existing order is in pending; a valid Admin JWT fixture is available; record the target order state before the request. | Authentication and Admin authorization permit the request to reach the documented pending to confirmed transition behavior. The target order is allowed to change to confirmed. No exact HTTP status, response body, headers, or message is asserted. | X-Student-Id header presence; fixture-backed target state |
| API-071 | SECURITY | 1 | A controlled existing order is in pending; record its state before the request. | The operation is not authorized and the target order remains pending. No exact HTTP status, response body, message, or authentication-versus-validation precedence is asserted. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-072 | SECURITY | 1 | A controlled existing order is in pending; the test environment supplies an inert bearer value classified as a malformed JWT; record the order state before the request. | The malformed JWT does not authenticate the caller and the target order remains pending. No exact HTTP status, response schema, message, or validation precedence is asserted. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-073 | SECURITY | 1 | A controlled existing order is in pending; the test environment supplies an inert JWT fixture classified as forged and invalid; record the order state before the request. | The forged invalid JWT does not authenticate the caller and the target order remains pending. No exact HTTP status, response schema, message, or validation precedence is asserted. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-074 | SECURITY | 1 | A controlled existing order is in pending; the test environment supplies an inert JWT fixture it classifies as expired and invalid; record the order state before the request. | The expired invalid JWT does not authenticate the caller and the target order remains pending. No exact HTTP status, response schema, message, or validation precedence is asserted. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-075 | SECURITY | 1 | A controlled existing order is in pending; the test environment supplies an inert bearer JWT fixture known to be invalid for an environment-defined reason other than the separately tested malformed, forged, and expired classes; record the order state before the request. | The environment-classified invalid JWT does not authenticate the caller and the target order remains pending. No exact HTTP status, response schema, message, or validation precedence is asserted. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-076 | SECURITY | 1 | A controlled existing order is in pending; a valid JWT fixture lacking role = 'admin' is available; record the order state before the request. | The Admin-role check denies the mutation and the target order remains pending. No exact denial status, body, message, or processing precedence is asserted. | X-Student-Id header presence; denial/error signal; fixture-backed target state |
| API-077 | SECURITY | 1 | A valid Admin JWT fixture is available; controlled fixtures allow before/after inspection of all potentially affected orders, including unrelated orders. | The inert id value does not execute query instructions, select or update unintended orders, or broaden the affected row set. No exact validation, rejection, not-found status, body, or message is asserted. | X-Student-Id header presence; fixture-backed target state; fixture-backed unrelated-order isolation |
| API-078 | SECURITY | 1 | A valid Admin JWT fixture is available; a controlled legitimate target order and unrelated orders can be inspected before and after the request. | The inert status value does not execute query instructions, place the target in an unintended state, bypass lifecycle constraints, or modify unrelated orders. No exact invalid-value status, body, schema, or message is asserted. | X-Student-Id header presence; fixture-backed target state; fixture-backed unrelated-order isolation |
| API-079 | SECURITY | 1 | A controlled target order is in pending; at least one unrelated controlled order exists; a valid Admin JWT fixture is available; record all controlled order states before the request. | Only the identified target order performs the documented pending to confirmed transition; unrelated orders remain unchanged. No exact success status, response body, headers, or implementation-level query construction is asserted. | X-Student-Id header presence; fixture-backed target state; fixture-backed unrelated-order isolation |
| API-080 | CONTRACT | 2 | A controlled order exists in pending state with a known ID. A valid Admin JWT is available. The same order is used for both ordered requests. | The first response is a safe `4xx`, not `5xx`, and does not change the order status. The second request succeeds and changes `pending → confirmed`. | X-Student-Id header presence; ordered step-specific 4xx/state assertions |
| API-081 | DOMAIN | 2 | A controlled order exists in pending state with a known ID. A valid Admin JWT is available. The same order is used for both ordered requests. | The first request returns a `4xx` error and does not change the order. The valid retry succeeds with `pending → confirmed`. | X-Student-Id header presence; ordered step-specific 4xx/state assertions |
| API-082 | DOMAIN | 2 | A controlled order exists in pending state with a known ID. A valid Admin JWT is available. The same order is used for both ordered requests. | The whitespace-modified value is rejected with a `4xx` and leaves the order pending. The exact-value retry succeeds. | X-Student-Id header presence; ordered step-specific 4xx/state assertions |
| API-083 | STATE | 2 | A controlled order exists in shipping state with a known ID. A valid Admin JWT is available. The same order is used for both ordered requests. | The first request is rejected with `4xx` and must not change the state. The second request succeeds, proving the invalid transition left the order in `shipping`. | X-Student-Id header presence; ordered step-specific 4xx/state assertions |
| API-084 | STATE | 4 | A controlled order exists in pending state with a known ID. A valid Admin JWT is available. The same persistent order is used for all four ordered requests. | The first three updates succeed in order. The final request is rejected with `4xx` because `delivered` is a final state. | X-Student-Id header presence; ordered step-specific 4xx/state assertions |
| API-085 | SECURITY | 2 | A controlled order exists in pending state with a known ID. Valid non-Admin and Admin JWT fixtures are available. The same order is used for both ordered requests. | The non-Admin request is rejected with a `4xx` and must not change the order. The Admin retry succeeds with `pending → confirmed`. | X-Student-Id header presence; ordered step-specific 4xx/state assertions |

## Traceability result

All API-001 through API-085 are represented exactly once as reviewed cases. Test ID, category, objective, preconditions, request input, expected result, specification basis, and assumptions/notes are preserved in schema-supported request or flow descriptions. No case was added, removed, merged, or redesigned.
